package model;

import model.Dish;

import java.io.*;

public class Main {
    public static void main(String[] args) throws IOException {
       /* Dish dish = new Dish();
        File file = new File("dish.obj");

        try (
                var outputStream = new FileOutputStream(file);
                var output = new ObjectOutputStream(outputStream)
        ) {
            output.writeObject(dish);
        } catch (IOException e) {
            e.printStackTrace();
        }

        try (
                var inputStream = new FileInputStream(file);
                var input = new ObjectInputStream(inputStream);
        ) {
            var object = (Dish) input.readObject();
            System.out.println(object);
        } catch (FileNotFoundException | ClassNotFoundException e) {
            e.printStackTrace();
        }
*/
    }
}
